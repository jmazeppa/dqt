# Repository Guidelines

## プロジェクト構成とモジュール

このリポジトリは、ビルド不要の静的サイトです。ルート直下の各HTMLファイルが独立したブラウザツールになっています。

- `arenor.html`: 闘技場のまとめ画像を生成します。READMEに記載されたステージングページです。
- `gacha.html`: ガチャ画像を連結します。
- `status.html`: キャラクターのステータスを計算し、`status.json` のデータを読み込みます。
- `self_calc.html`: 基礎値を自由入力し、完凸換算・理論値・戦闘中ステータスを計算します。
- `README.md`: 公開URL、ステージングURL、ステータスデータの算出ルールを記載します。
- `CHANGELOG.md`: 利用者に影響する変更を時系列で記録します。
- `knowledge/`: ステータス計算など、実装と実測から得た仕様・制約を記録します。

ページ固有のHTML、CSS、JavaScriptは、共有する明確な理由がない限り同じHTML内に保ってください。ステータスのスキーマを変更する場合は、`status.json` と読み込み側の `status.html` を同時に更新します。

## サービス構成と目的

本番環境は Google Sites の `https://dqt.mazeppa.jp/` です。GitHub Pages の `https://jmazeppa.github.io/dqt/` はステージング環境として使用し、公開前の動作確認を行います。本プロジェクトは『ドラゴンクエストタクト』（DQT）の自分用便利ツールを提供し、公式サイトは `https://www.dragonquest.jp/tact/`、既存の参考ツールは `https://tact-research.net/` です。

`status.json` のキャラクターデータは約8か月前の内容で、現在はランク9相当です。直近の目標は、ランク9データを保持しつつランク10パラメータをJSON化し、自由入力に対応することです。将来的にはGoogleスプレッドシートの入力値からアプリ経由で `status.json` をGitHubへ自動反映する構想がありますが、未実装です。データ形式や自動化方式など、仕様が不明な場合は推測で実装せず、管理者に確認してください。

## ビルド・テスト・開発用コマンド

パッケージ管理やビルド処理はありません。デプロイ時と同様にデータを読み込めるよう、HTTPサーバーを起動します。

```sh
python3 -m http.server 8000
```

ブラウザで `http://localhost:8000/arenor.html`、`gacha.html`、または `status.html` を開いてください。ステージング確認ではGitHub Pagesへ反映し、対象ページを実機でも確認します。コミット前に次のコマンドを実行します。

```sh
python3 -m json.tool status.json >/dev/null
git diff --check
```

前者はJSONの構文を検証し、後者は空白エラーを検出します。あわせて `git diff` で意図しない変更がないか確認してください。

## コーディング規約と命名

HTML、CSS、JavaScriptでは既存の4スペースインデントを維持します。JavaScriptの識別子はcamelCase、CSSクラスはkebab-case、JSONキーはsnake_caseを使用してください。画面文言や説明コメントは周辺のコードに合わせて日本語で記述します。`rem`、パーセント、`clamp()` などを使い、モバイル表示とiframe埋め込みへの対応を保ってください。本サービスはiPhone 16などのスマートフォン利用を主対象とします。タップ領域、横スクロール、入力時の拡大、画面幅からのはみ出しに注意し、モバイル表示を優先して設計してください。ステータス配列は既存の6要素の並び順を変更しないでください。

## テスト方針

自動テストやカバレッジ基準は未設定です。変更した操作はiPhone 16相当のモバイル幅を中心に、デスクトップ幅でも手動確認します。画像ツールでは、ファイル選択、プレビュー、各設定、Canvas生成、ダウンロードを確認してください。ステータス機能では、コンソールエラーがないこと、データ読み込み、検索、選択、覚醒段階ごとの再計算を確認します。HTMLファイルの直接表示ではなく、ローカルHTTPサーバーを使用してください。

## コミットとプルリクエスト

直近の履歴では、`Logic check` や `Update character selection and version info` のような短く命令形の要約が使われています。この形式に合わせ、1コミットの目的を絞ってください。プルリクエストには、利用者への影響、変更したページやデータ、実施した手動確認、関連Issueを記載します。レイアウトや生成画像を変更した場合は、特にモバイル幅での変更前後のスクリーンショットを添付してください。

利用者に影響するコード、データ、設定、文書をコミットする際は、同じコミットで `CHANGELOG.md` の「未リリース」欄を更新してください。各項目には `` `[対象: status.html]` `` の形式で変更ファイルを必ず明記します。HTMLを変更する場合はページ内のバージョンと最終更新日も更新し、CHANGELOGにもバージョンを記録してください。過去の記録は書き換えず、追加・変更・修正など適切な分類へ簡潔に追記します。
